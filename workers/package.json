{
  "name": "bitbot-workers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler dev --port 8787 --local",
    "deploy": "npx wrangler deploy",
    "deploy:dev": "npx wrangler deploy --env dev",
    "dev:dev": "npx wrangler dev --env dev --port 8788 --local"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.0",
    "@larksuiteoapi/node-sdk": "^1.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^6.0.3",
    "wrangler": "^3.0.0"
  }
}
